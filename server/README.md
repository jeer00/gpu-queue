# Server

Import the homeserver stack into this folder from:

```text
homeserver:/home/homeserver/data/rabbitmq-gpu-stack
```

Do not commit live `.env` files, uploaded media, generated results, database
files, passwords, keys, or LAN IP addresses.
